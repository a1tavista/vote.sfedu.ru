<form action="/sign_in" method="post" class="welcome-page__authorize">
  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
  <input type="text" name="user[identity_url]" id="user_identity_url">
  <button>Войти</button>
</form>
